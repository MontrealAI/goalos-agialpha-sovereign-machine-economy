
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>Array.from(r.querySelectorAll(s));
function aura(){const c=$('.aura');if(!c)return;const x=c.getContext('2d');let w,h,d=1,p=[];function rs(){d=Math.min(devicePixelRatio||1,2);w=c.width=innerWidth*d;h=c.height=innerHeight*d;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';p=Array.from({length:Math.min(150,Math.floor(innerWidth/11))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.18*d,vy:(Math.random()-.5)*.18*d,r:(Math.random()*1.6+.4)*d,a:Math.random()*6.28}))}function dr(){x.clearRect(0,0,w,h);for(const q of p){q.x=(q.x+q.vx+w)%w;q.y=(q.y+q.vy+h)%h;q.a+=.01;x.beginPath();x.arc(q.x,q.y,q.r,0,Math.PI*2);x.fillStyle=`rgba(103,247,223,${.08+Math.sin(q.a)*.05})`;x.fill()}for(let i=0;i<p.length;i++)for(let j=i+1;j<p.length;j++){const a=p[i],b=p[j],dx=a.x-b.x,dy=a.y-b.y,di=Math.hypot(dx,dy);if(di<115*d){x.strokeStyle=`rgba(114,168,255,${(1-di/(115*d))*.07})`;x.lineWidth=d;x.beginPath();x.moveTo(a.x,a.y);x.lineTo(b.x,b.y);x.stroke()}}requestAnimationFrame(dr)}addEventListener('resize',rs,{passive:true});rs();dr()}
const scenarios={proof:{base:34,title:'Proof Run 001',mission:'Demonstrate the complete GoalOS loop from mission to verified experience, Chronicle, capability package, settlement simulation, and reinvestment trigger.'},frontier:{base:28,title:'Frontier Release Governance',mission:'Convert high-consequence frontier model release disputes into Evidence Dockets, validator seats, access-tier matrices, rollback conditions, and human-authorized decision states.'},coordination:{base:31,title:'Multi-Agent Institution',mission:'Coordinate agents as a proof-governed institution, not a swarm: bounded jobs, validators, memory, settlement, reuse, and reinvestment.'},research:{base:38,title:'Research Spine',mission:'Map AGI ALPHA, Mission OS, and the Proof-of-Evolution Constitution into product surfaces, standards, schemas, and evaluation programs.'}};
const gates=['Mission','Allocation','Work','Proof','Validation','Decision','Chronicle','Capability','Settlement','Reinvestment'];
const controls={mission:{label:'Mission Contract',boost:8},docket:{label:'Evidence Docket',boost:13},validator:{label:'Validator Report',boost:14},risk:{label:'Risk Ledger',boost:7},replay:{label:'Replay Path',boost:9},settlement:{label:'Settlement Simulation',boost:8},reuse:{label:'Capability Package',boost:10},human:{label:'Human Authority',boost:6}};
let state={scenario:'proof',gate:0,controls:new Set(['mission','docket','human']),timer:null};
const pad=n=>String(n).padStart(2,'0'),now=()=>{const d=new Date();return `${pad(d.getHours())}:${pad(d.getMinutes())}:${pad(d.getSeconds())}`};
function score(){let s=scenarios[state.scenario].base;state.controls.forEach(k=>s+=controls[k].boost);s+=state.gate*2.2;return Math.max(15,Math.min(97,Math.round(s)))}
function decision(s){return s>=86?'READY_FOR_EXTERNAL_REVIEW':s>=74?'REVIEW_READY_WITH_BOUNDARY':s>=58?'HOLD_PENDING_STRUCTURED_REVIEW':'HOLD_PENDING_EVIDENCE'}
function log(m,c=''){const t=$('#chronicle');if(!t)return;const d=document.createElement('div');d.className=c;d.textContent=`${now()}  ${m}`;t.appendChild(d);t.scrollTop=t.scrollHeight}
function graph(){const svg=$('#graph');if(!svg)return;const cx=365,cy=205,r=150;let h='<defs><filter id="glow"><feGaussianBlur stdDeviation="4" result="g"/><feMerge><feMergeNode in="g"/><feMergeNode in="SourceGraphic"/></feMerge></filter></defs><rect width="100%" height="100%" fill="transparent"/>';gates.forEach((g,i)=>{const a=Math.PI*2*i/gates.length-Math.PI/2,x=cx+Math.cos(a)*r,y=cy+Math.sin(a)*r,done=i<=state.gate;h+=`<line x1="${cx}" y1="${cy}" x2="${x}" y2="${y}" stroke="${done?'#67f7df':'rgba(255,255,255,.13)'}" stroke-width="${done?2.2:1}"/><circle cx="${x}" cy="${y}" r="26" fill="${done?'rgba(103,247,223,.16)':'rgba(255,255,255,.04)'}" stroke="${done?'#67f7df':'rgba(255,255,255,.2)'}" ${done?'filter="url(#glow)"':''}/><text x="${x}" y="${y+4}" fill="#fff" font-size="10" font-weight="900" text-anchor="middle">${g.slice(0,3).toUpperCase()}</text>`});h+=`<circle cx="${cx}" cy="${cy}" r="68" fill="rgba(5,10,20,.96)" stroke="#f5cc72" stroke-width="2" filter="url(#glow)"/><text x="${cx}" y="${cy+16}" text-anchor="middle" fill="#ffe8a7" font-size="58" font-weight="900">α</text>`;svg.innerHTML=h}
function render(){if(!$('#score'))return;const sc=scenarios[state.scenario],s=score();$('#scenario-title').textContent=sc.title;$('#mission').value=sc.mission;$('#score b').textContent=s;$('#score').style.setProperty('--score',s+'%');$('#decision').textContent=decision(s);$('#gate-title').textContent=gates[state.gate];$('#gate-note').textContent=`Gate ${state.gate+1}: ${gates[state.gate]} is inspected before proof can influence memory, settlement, or future work.`;$('#chain').textContent=`GOALOS-${state.scenario}-${s}-${state.gate}`;['proof','risk','reuse'].forEach((k,i)=>{const v=Math.min(100,s-(i*8)+(state.controls.has(k==='proof'?'docket':k)?10:0));const e=$('#bar-'+k);if(e)e.style.width=v+'%';const val=$('#val-'+k);if(val)val.textContent=v+'%'});$('#chips').innerHTML=Object.entries(controls).map(([k,v])=>`<label class="chip ${state.controls.has(k)?'active':''}" data-k="${k}"><input type="checkbox" ${state.controls.has(k)?'checked':''}>${v.label}</label>`).join('');$('#gates').innerHTML=gates.map((g,i)=>`<button class="gate ${i===state.gate?'active':''} ${i<state.gate?'done':''}" data-i="${i}"><b>${pad(i+1)}</b>${g}</button>`).join('');graph()}
function docket(){return{schema:'goalos.master.docket.v1',generated_at:new Date().toISOString(),scenario:scenarios[state.scenario].title,mission:$('#mission')?.value,score:score(),decision:decision(score()),controls:Array.from(state.controls),gate:gates[state.gate],authority_boundary:['no_model_call','no_wallet','no_transaction','no_production_authorization','human_review_required']}}
function download(n,b,t){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([b],{type:t}));a.download=n;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),800)}
function bindConsole(){if(!$('#run-flight'))return;$('#scenario').onchange=e=>{state.scenario=e.target.value;state.gate=0;render();log('Scenario changed: '+scenarios[state.scenario].title,'warn')};$('#chips').onchange=e=>{const c=e.target.closest('.chip');if(!c)return;const k=c.dataset.k;e.target.checked?state.controls.add(k):state.controls.delete(k);render();log('Control updated: '+controls[k].label,e.target.checked?'good':'warn')};$('#gates').onclick=e=>{const g=e.target.closest('.gate');if(!g)return;state.gate=Number(g.dataset.i);render();log('Gate inspected: '+gates[state.gate],'good')};$('#run-flight').onclick=()=>{clearInterval(state.timer);$('#chronicle').innerHTML='';state.gate=0;render();log('Mission sealed for browser-local proof flight.','warn');let i=0;state.timer=setInterval(()=>{state.gate=i;render();log(`Gate ${pad(i+1)} passed: ${gates[i]}`,i<7?'good':'warn');i++;if(i>=gates.length){clearInterval(state.timer);log('HUMAN_REVIEW_REQUIRED: package ready for review.','bad')}},520)};$('#stress').onclick=()=>{state.gate=Math.floor(Math.random()*gates.length);render();log('Stress review simulated.','warn')};$('#download-docket').onclick=()=>download('goalos-master-docket.json',JSON.stringify(docket(),null,2),'application/json');$('#download-brief').onclick=()=>download('goalos-master-review-brief.md',`# GoalOS Review Brief\n\nScenario: ${docket().scenario}\n\nDecision: ${docket().decision}\n\nScore: ${docket().score}\n\nBoundary: human review required.`, 'text/markdown');render();log('GoalOS master console initialized.','good')}
document.addEventListener('DOMContentLoaded',()=>{aura();bindConsole()});
