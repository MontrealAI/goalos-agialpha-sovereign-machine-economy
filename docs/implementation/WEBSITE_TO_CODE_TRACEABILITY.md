# Website-to-Code Traceability

The website is the proof interface. The repository code is the reference implementation. The parity report maps every major website capability to code, tests, reports, and generated Evidence Docket artifacts.

The critical rule is simple: the website may present a public-alpha capability only if the repository contains a corresponding implementation artifact and a claim boundary.
