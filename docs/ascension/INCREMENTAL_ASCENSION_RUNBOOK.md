# Incremental Ascension Autopilot Runbook

Upload `.github` and `.goalos`, run **GoalOS Incremental Ascension Autopilot v65**, keep `allow_overwrite=false`, and deploy Pages. The installer is additive and patches existing pages only inside explicit markers.
