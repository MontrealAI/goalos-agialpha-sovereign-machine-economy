# How to Run Replay

Start with the repository scripts:

```bash
python -m pip install -e .
python -m pip install pytest
python -m pytest
python scripts/run_proof_run_001.py
python scripts/run_website_code_parity_v3.py
```

Record expected output, actual output, environment, and deviations.
