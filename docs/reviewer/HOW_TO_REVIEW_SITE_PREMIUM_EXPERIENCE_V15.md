# How to review GoalOS Site Premium Experience V15

1. Confirm all pages remain discoverable from `site-map.html`.
2. Confirm `index.html` is a useful command center, not a generic generated page.
3. Press `/` and search for `RSI`, `loop`, `token`, `docket`, `validator`, `mission`.
4. Confirm the trust/token/data boundary appears on the homepage and boundary pages.
5. Confirm no page asks for user data, funds, wallet, transaction, or production authority.
6. Confirm reports show `status: passed`.
