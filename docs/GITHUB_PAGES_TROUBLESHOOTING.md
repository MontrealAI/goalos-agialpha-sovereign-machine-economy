# GitHub Pages Troubleshooting

If the Run workflow button is missing, confirm the workflow file is on the default branch and contains workflow_dispatch. If Pages is not live, confirm Settings → Pages uses GitHub Actions and the deploy job finished.

## Operating rule

Every strong claim must remain connected to evidence, validation, risk review, and human authorization where appropriate.
